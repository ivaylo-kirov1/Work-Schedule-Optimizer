
export const C_MAX = 5 // mirrors weights.py C_MAX — max consecutive working days
export const DELTA_PER_MONTH = 8 // mirrors weights.py DELTA_PER_MONTH — S2 tolerance band per month
export const CP_SAT_EMPLOYEE_THRESHOLD = 15 // UI heuristic — CP-SAT practical size limit (~15 employees), not a weights.py value
export const NIGHT_CAP_HOURS = 7 // mirrors the FIVE_DAY night-shift hour cap 
